
#include <stdio.h>
#include <iostream>
#include <fstream>
#include <vector>
#include <functional>
#include <string>
#include <queue> 
#include <map> 
#include <stack> 
#include <set> 
using namespace std;

// on mac: clang++ -std=c++11 -stdlib=libc++ xxx.cc && ./a.out

int main() {
    cout << atoi("123") << endl;
    cout << atoi("a123") << endl;
    cout << atoi("12a3") << endl;
    cout << atoi("-123") << endl;
    return 0;
}

